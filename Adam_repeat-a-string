function repeatStringNumTimes(str, num) {
  var repStr = "";
  if (num < 0) {
    return "";
  }
  else {
    for (var i = 0; i < num; i ++) {
    repStr = repStr.concat(str);
    }
  }
  return repStr;
}
